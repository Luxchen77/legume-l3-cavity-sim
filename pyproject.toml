[project]
name = "photonics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "legume-gme>=1.0.2",
    "numpy>=1.26",
    "scipy",
    "matplotlib",
    "autograd",
    "h5py",
    "jupyter",
]
